<%= form_with(model: text_file, local: true) do |form| %>
  <% if text_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(text_file.errors.count, "error") %> prohibited this text_file from being saved:</h2>

      <ul>
      <% text_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :text_file_name %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :text_file_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
